<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="field">
    <%= f.label :Tecnologia %><br>
    <%= f.select :technology, options_for_select([["Escolha a Tecnologia"],["VB"], ["C"], ["DotNet"]]) %>

  </div>
  <div class="field">
    <label class="checkbox-inline">
    <%= collection_check_boxes(:product, :source_ids, @sources, :id, :nome) %>
    </label>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
